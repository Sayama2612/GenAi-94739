import math_utils

len = float(input("Enter length of rectangle:"))
br = float(input("Enter breadth of rectangle:"))

math_utils.calc_rect_area(len, br)

side = float(input("Enter side of square:"))
math_utils.calc_square_area(side)